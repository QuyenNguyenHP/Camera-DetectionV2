{
  "name": "vision-guard",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,backend -c cyan,green \"npm --prefix frontend run dev\" \"npm run dev:backend\"",
    "dev:backend": "cd backend && python3 app.py",
    "install:all": "npm install --prefix frontend",
    "test": "npm --prefix frontend run test && cd backend && .venv/bin/python -m pytest -q"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
